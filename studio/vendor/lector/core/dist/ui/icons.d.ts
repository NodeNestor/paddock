/**
 * Built-in SVG icons for the Lector viewer UI.
 *
 * Icons sourced from Lucide (https://lucide.dev) — ISC License.
 * Copyright (c) for portions of Lucide are held by Cole Bemis 2013-2022
 * as part of Feather Icons (MIT License).
 * Copyright (c) for Lucide contributors 2022-present (ISC License).
 *
 * Only the specific SVG strings needed by the default UI schema are
 * included here — no runtime dependency on the lucide package.
 */
/** Look up a built-in icon SVG string by name. */
export declare function getIcon(name: string): string | undefined;
/** Check if a string is an inline SVG. */
export declare function isInlineSvg(ref: string): boolean;
/** Resolve an icon reference to an SVG string. */
export declare function resolveIcon(ref: string | undefined): string | undefined;
//# sourceMappingURL=icons.d.ts.map